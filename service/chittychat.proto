syntax = "proto3";

option go_package = "disysminiproject2/service";

package chittychat;


service chittychat {
    rpc ChatSession(stream Message) returns (stream Message) {};
}


message Message {
    uint64 clock = 1;
    string message = 2;
}

